class AppConstants {
  // Use production URL by default, or localhost for dev if needed
  static const String apiBaseUrl = 'https://auth.medixalink.com'; 
  static const String socketUrl = 'https://auth.medixalink.com';
}
